# OpenAI API Configuration

# API ключ для доступа к OpenAI или ProxyAPI (обязательный параметр)
# Для ProxyAPI используйте API ключ от сервиса ProxyAPI, а не от OpenAI
OPENAI_API_KEY=your_api_key_here

# Base URL для ProxyAPI (обязательно для ProxyAPI!)
# Если используется стандартный OpenAI API, оставьте пустым или удалите эту строку
# Примеры для ProxyAPI:
#   - https://api.proxyapi.ru/openai/v1
#   - https://api.bothub.chat/v1
#   - другой URL вашего прокси-сервиса
OPENAI_BASE_URL=

# Модель для использования (по умолчанию gpt-4o-mini)
# Доступные модели: gpt-4o-mini, gpt-4o, gpt-3.5-turbo и другие
OPENAI_MODEL=gpt-4o-mini

# Параметр temperature (0.0 - 1.0)
# Чем выше значение, тем более креативные и разнообразные ответы
# 0.0 - детерминированные ответы, 1.0 - максимальная креативность
OPENAI_TEMPERATURE=0.7

# Максимальное количество токенов в ответе
# Рекомендуется 1000-2000 для большинства задач
OPENAI_MAX_TOKENS=2000

