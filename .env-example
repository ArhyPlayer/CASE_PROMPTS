# API ключ для доступа к OpenAI (обязательно!)
# Если используется ProxyAPI, укажите ключ от прокси-сервиса
OPENAI_API_KEY=your_actual_api_key_here

# Base URL для ProxyAPI (опционально)
# Если используется ProxyAPI, укажите URL, например:
# OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
# Если не используется, оставьте пустым или закомментируйте
OPENAI_BASE_URL=

# Модель OpenAI (по умолчанию gpt-4o-mini - оптимальная по цене/качеству)
# Возможные варианты: gpt-4o-mini, gpt-3.5-turbo, gpt-4o, gpt-4-turbo
OPENAI_MODEL=gpt-4o-mini

# Temperature - креативность ответов (0.0-1.0)
# 0.0-0.3: детерминированные ответы
# 0.4-0.6: сбалансированный подход
# 0.7-0.9: креативные ответы
OPENAI_TEMPERATURE=0.7

# Максимальное количество токенов в ответе
# 1000 токенов ≈ 750 слов
# Рекомендуется 1000-2000 для большинства задач
OPENAI_MAX_TOKENS=2000

# Токен Telegram бота (для генератора ботов)
# Получите токен у @BotFather в Telegram
BOT_TOKEN=your_telegram_bot_token_here

# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# DEBUG - максимум информации (для отладки)
# INFO - основная информация о работе (рекомендуется)
# WARNING - только предупреждения и ошибки
# ERROR - только ошибки
LOG_LEVEL=INFO
