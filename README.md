# 🤖 CLI Инструмент для работы с промптами и OpenAI API

Интерактивный CLI-инструмент для работы с заготовленными промптами и генерации ответов через OpenAI API (с поддержкой ProxyAPI).

## 📋 Описание

Скрипт предоставляет удобный интерфейс командной строки для:
- Загрузки и управления промптами из JSON-файлов
- Интерактивного выбора промптов
- Настройки параметров модели (temperature, max_tokens)
- Отправки запросов к OpenAI API через ProxyAPI
- Получения детальной статистики по использованию токенов
- **Генерации Telegram ботов с использованием LangChain** (для промпта "Генерация структуры кода")

## 🏗️ Структура проекта

```
CASE_PROMPTS/
├── prompts/                    # Директория с JSON-файлами промптов
│   ├── 01_summary_prompt.json
│   ├── 02_code_structure.json
│   └── 03_task_planning_prompt.json
├── venv/                       # Виртуальное окружение Python
├── common_prompts.py           # Основной исполняемый скрипт
├── script_bot.py               # Генератор Telegram ботов (LangChain)
├── requirements.txt            # Зависимости проекта
├── .env-example               # Пример файла с переменными окружения
├── .env                       # Ваш файл с настройками (создается вручную)
└── README.md                  # Документация проекта
```

## ✨ Ключевые особенности

### Основные возможности
- 📋 **Управление промптами** - загрузка и использование готовых промптов из JSON
- 🎯 **Интерактивный выбор** - удобное CLI меню с подсказками
- ⚙️ **Настройка параметров** - контроль temperature, max_tokens, выбор модели
- 🌐 **Поддержка ProxyAPI** - работа через прокси-серверы
- 📊 **Детальная статистика** - информация об использовании токенов

### Генератор Telegram ботов (LangChain)
- 🤖 **4-этапная цепочка** - последовательная обработка через специализированные агенты
- 🧠 **Интеллектуальный анализ** - глубокое понимание требований к боту
- 🔧 **Умный подбор инструментов** - автоматический выбор технологий и библиотек
- 🏗️ **Архитектурное проектирование** - создание продуманной структуры кода
- 💻 **Полная реализация** - готовый к запуску код без заглушек
- ✅ **Автоматическая проверка** - валидация качества кода

## 🚀 Установка и настройка

### 1. Установка зависимостей

```bash
# Активируйте виртуальное окружение (если еще не активировано)
source venv/bin/activate  # для Linux/macOS
# или
venv\Scripts\activate  # для Windows

# Установите необходимые пакеты
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env-example`:

```bash
cp .env-example .env
```

Откройте `.env` и заполните необходимые параметры:

```env
# API ключ для доступа к OpenAI (обязательно!)
OPENAI_API_KEY=your_actual_api_key_here

# Base URL для ProxyAPI (опционально)
# Если используется ProxyAPI, укажите URL, например:
# OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
OPENAI_BASE_URL=

# Модель (по умолчанию gpt-4o-mini - оптимальная по цене/качеству)
OPENAI_MODEL=gpt-4o-mini

# Temperature - креативность ответов (0.0-1.0)
OPENAI_TEMPERATURE=0.7

# Максимальное количество токенов в ответе
OPENAI_MAX_TOKENS=2000

# Токен Telegram бота (для генератора ботов)
BOT_TOKEN=your_telegram_bot_token_here

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
```

## 💻 Использование

### Запуск скрипта

```bash
python common_prompts.py
```

### Многострочный ввод текста

Скрипт поддерживает **три способа ввода** для вопросов:

#### 1. Прямой ввод / Вставка текста
1. Когда программа попросит ввести вопрос, вставьте любой текст (даже многострочный)
2. Для завершения ввода:
   - **Напишите END на новой строке**, или
   - **Нажмите Ctrl+D** (на Mac/Linux) / **Ctrl+Z** (на Windows)

#### 2. Чтение из файла (РЕКОМЕНДУЕТСЯ для больших текстов)
Если у вас очень большой текст, сохраните его в файл и используйте команду:
```
file:/путь/к/вашему/файлу.txt
```

Пример:
```
💬 Введите ваш вопрос:
💡 Варианты ввода:
   1. Вставьте текст, затем на новой строке напишите: END
   2. Нажмите Ctrl+D (Mac/Linux) / Ctrl+Z (Windows) для завершения
   3. Введите 'file:путь_к_файлу.txt' для чтения из файла
--------------------------------------------------------------------------------
file:~/Documents/my_text.txt
✅ Прочитано из файла: 5432 символов
--------------------------------------------------------------------------------
```

#### 3. Многострочная вставка
```
💬 Введите ваш вопрос:
--------------------------------------------------------------------------------
Здесь ваш длинный текст
может быть на нескольких строках
сколько угодно...
END
✅ Введено строк: 3, символов: 120
--------------------------------------------------------------------------------
```

**Совет:** При вставке очень больших текстов (более 1000 строк) используйте чтение из файла для надежности.

### Интерактивный процесс работы

1. **Выбор тестового режима**
   - Программа спросит, хотите ли вы использовать тестовый вопрос

2. **Настройка параметров модели**
   - Temperature (0.0-1.0): контролирует креативность ответов
   - Max tokens: максимальная длина ответа
   - Модель: выбор модели GPT

3. **Выбор промпта**
   - Программа покажет список доступных промптов
   - Введите номер желаемого промпта (1-3) или "выход" для завершения
   - **Для промпта "Генерация структуры кода" доступно подменю:**
     - Стандартные запросы
     - Генерация Telegram бота (LangChain)

4. **Ввод вопроса**
   - Можно использовать тестовый вопрос из промпта
   - Или ввести свой собственный вопрос (поддерживается многострочный ввод)
   - Для завершения многострочного ввода: напишите END на новой строке или Ctrl+D (Mac/Linux) / Ctrl+Z (Windows)

5. **Получение ответа**
   - Программа отправит запрос к OpenAI
   - Выведет полученный ответ
   - Покажет статистику использования токенов

### Пример работы

```
🤖 CLI Инструмент для работы с промптами и OpenAI API
================================================================================

😊 Использовать тестовый вопрос? (y/n, по умолчанию n): n

⚙️ Настройки модели:
🌡️ Введите temperature (0.0-1.0, по умолчанию 0.7): 
📊 Введите max_tokens (по умолчанию 2000): 
🤖 Введите модель (по умолчанию gpt-4o-mini): 

✅ Загружено промптов: 3

📋 Доступные промпты:
================================================================================

1. Резюме текста
   🔖 ID: summary
   📁 Категория: Анализ и обработка текста
   📝 Описание: Профессиональное создание кратких резюме...
   
2. Генерация структуры кода
   🔖 ID: code_structure
   📁 Категория: Разработка программного обеспечения
   ...

📋 Выберите промпт (1-3) или 'выход' для завершения: 1

✅ Выбран промпт: Резюме текста

💡 Доступен тестовый вопрос:
   Искусственный интеллект постепенно проникает...

😊 Использовать тестовый вопрос? (y/n, по умолчанию n): y

🔄 Отправляем запрос к OpenAI...

💡 Ответ:
================================================================================

[Ответ от OpenAI]

📊 Информация о запросе:
  • Модель: gpt-4o-mini-2024-07-18
  • Использовано токенов: 847
  • Промпт токены: 559
  • Ответ токены: 288

👍 Готово! До свидания!
```

## 📝 Формат промптов

Промпты хранятся в формате JSON в директории `prompts/`. Каждый промпт содержит:

```json
{
  "prompt_id": "уникальный_идентификатор",
  "name": "Название промпта",
  "category": "Категория",
  "version": "1.0",
  "description": "Описание назначения промпта",
  "role": "Роль и контекст для AI-ассистента",
  "context": "Контекст использования",
  "structure": { ... },
  "format": { ... },
  "examples": { ... },
  "usage_instructions": { ... },
  "test_input": "Тестовый вопрос для проверки",
  "expected_test_output_description": "Описание ожидаемого результата"
}
```

## 🔧 Поддерживаемые модели

Рекомендуемые модели GPT (от более доступных к более дорогим):
- **gpt-4o-mini** (рекомендуется) - оптимальное соотношение цена/качество
- **gpt-3.5-turbo** - быстрая и недорогая модель
- **gpt-4o** - более мощная модель для сложных задач
- **gpt-4-turbo** - самая продвинутая модель

## ⚙️ Параметры модели

### Temperature (0.0 - 1.0)
- **0.0-0.3**: Детерминированные, предсказуемые ответы (для технических задач)
- **0.4-0.6**: Сбалансированный подход
- **0.7-0.9**: Креативные, разнообразные ответы (для творческих задач)
- **1.0**: Максимальная креативность и случайность

### Max Tokens
- Определяет максимальную длину ответа
- 1000 токенов ≈ 750 слов (для английского)
- Рекомендуется 1000-2000 для большинства задач

## 🌐 Работа с ProxyAPI

Если вы используете ProxyAPI для доступа к OpenAI, выполните следующие шаги:

### 1. Получите ключ API от ProxyAPI сервиса

**Важно!** Для ProxyAPI нужен API ключ от самого прокси-сервиса, а НЕ от OpenAI.

Популярные ProxyAPI сервисы:
- [ProxyAPI.ru](https://proxyapi.ru)
- [BothHub](https://bothub.chat)
- Другие сервисы-посредники

### 2. Укажите настройки в `.env`:

```env
# API ключ от вашего ProxyAPI сервиса (не от OpenAI!)
OPENAI_API_KEY=ваш_ключ_от_proxyapi

# Base URL вашего ProxyAPI сервиса (обязательно!)
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1

# Остальные настройки
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=2000
```

### Примеры Base URL для разных сервисов:

```env
# ProxyAPI.ru
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1

# BothHub
OPENAI_BASE_URL=https://api.bothub.chat/v1

# Другой сервис - уточните в документации
OPENAI_BASE_URL=https://your-proxy-service.com/v1
```

Скрипт автоматически использует указанный proxy-сервер для всех запросов.

## ⚠️ Требования

### Системные требования
- Python 3.8+
- Операционная система: Windows, Linux, macOS
- Доступ к интернету

### Необходимые компоненты
- Виртуальное окружение Python (рекомендуется)
- API ключ OpenAI или ProxyAPI
- Для генерации ботов: дополнительно потребуется токен от @BotFather (Telegram)

### Зависимости Python
Все зависимости указаны в `requirements.txt`:
- `openai` - OpenAI API клиент
- `python-dotenv` - Управление переменными окружения
- `langchain` - Фреймворк для цепочек LLM
- `langchain-openai` - Интеграция OpenAI для LangChain
- `langchain-core` - Ядро LangChain

Для запуска сгенерированных ботов дополнительно:
- `aiogram` (версия 3.x) - Асинхронный фреймворк для Telegram ботов

## 🐛 Устранение неполадок

### Ошибка: "OPENAI_API_KEY не найден"
Убедитесь, что:
1. Файл `.env` создан в корне проекта
2. В `.env` указан валидный API ключ
3. Файл `.env` не игнорируется Git

### Ошибка: "Директория prompts не найдена"
Убедитесь, что директория `prompts/` существует и содержит JSON-файлы

### Ошибка подключения к API
Проверьте:
1. Наличие интернет-соединения
2. Правильность API ключа
3. Корректность OPENAI_BASE_URL (если используется ProxyAPI)
4. Наличие средств на балансе API

### Ошибка: "script_bot.py не найден"
- Убедитесь, что вы находитесь в корневой директории проекта
- Проверьте наличие файла командой: `dir` (Windows) или `ls` (Linux/macOS)

### Ошибка при запуске бота: "BOT_TOKEN не найден"
- Добавьте токен в `.env` файл: `BOT_TOKEN=ваш_токен`
- Получите токен у @BotFather в Telegram
- Перезапустите бота после добавления токена

### LangChain ошибки
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте версии пакетов: `pip list | findstr langchain` (Windows) или `pip list | grep langchain` (Linux/macOS)
- При необходимости обновите пакеты: `pip install --upgrade langchain langchain-openai langchain-core`

## 🤖 Генерация Telegram ботов с LangChain

### Описание

При выборе промпта **"Генерация структуры кода"** доступна специальная функция генерации Telegram ботов с использованием LangChain.

### Как это работает

Генератор использует **последовательную цепочку из 4 этапов** (LangChain Pipeline):

#### 🔄 Pipeline генерации бота

```
Описание бота
     ↓
┌─────────────────────────────┐
│ 1. Analysis Chain           │ ← Анализ задания
│    (Бизнес-аналитик)        │   Определение цели, функций,
└─────────────────────────────┘   сложности
     ↓
┌─────────────────────────────┐
│ 2. Tools Selection Chain    │ ← Подбор инструментов
│    (Архитектор решений)     │   Framework, БД, библиотеки,
└─────────────────────────────┘   middleware
     ↓
┌─────────────────────────────┐
│ 3. Structure Chain          │ ← Создание структуры
│    (Senior разработчик)     │   Команды, handlers, FSM,
└─────────────────────────────┘   клавиатуры, модули
     ↓
┌─────────────────────────────┐
│ 4. Code Chain               │ ← Реализация кода
│    (Expert разработчик)     │   Полный рабочий код
└─────────────────────────────┘   на aiogram 3.x
     ↓
┌─────────────────────────────┐
│ 5. Review Chain             │ ← Финальная проверка
│    (Код-ревьюер)            │   Валидация и качество
└─────────────────────────────┘
     ↓
  generated_bot.py
```

#### Детальное описание этапов

**Этап 1: Анализ задания бота**
- Определение основного назначения
- Выявление ключевых функций
- Анализ типов взаимодействия с пользователем
- Оценка уровня сложности (simple/medium/complex)
- Выявление особых требований

**Этап 2: Подбор инструментов для генерации**
- Выбор версии aiogram (3.x)
- Определение необходимости базы данных (sqlite/postgresql/none)
- Подбор дополнительных библиотек (requests, pillow и т.д.)
- Определение API интеграций
- Выбор middleware компонентов (логирование, антиспам)
- Определение способа управления состояниями (FSM/memory/none)

**Этап 3: Создание структуры кода**
- Проектирование команд бота
- Определение обработчиков (command_handler, message_handler, callback_handler)
- Создание схемы состояний FSM (если нужны диалоги)
- Проектирование клавиатур (reply/inline)
- Структура модулей и файлов
- Модели данных
- Вспомогательные функции

**Этап 4: Реализация кода бота**
- Генерация полного рабочего кода
- Использование aiogram 3.x (Router, Dispatcher)
- Все handlers как async def
- Токен из переменной окружения BOT_TOKEN
- Логирование и обработка ошибок
- Готовность к запуску без модификаций

**Этап 5: Финальная проверка**
- Проверка синтаксических ошибок
- Корректность структуры запуска
- Правильность импортов
- Общее качество кода
- Рекомендации по улучшению

### Использование

Из главного меню:
```
📋 Выберите промпт: 2 (Генерация структуры кода)

📋 ПОДМЕНЮ: Генерация структуры кода
1. Стандартные запросы
2. Генерация Telegram бота (LangChain)

Выберите опцию: 2

💬 Введите описание бота (что он должен уметь):
Бот, который отправляет случайные мемы по команде /meme
END

Процесс генерации (4 этапа):
📊 ШАГ 1/4: Анализ задания бота...
✅ Анализ завершен
🔧 ШАГ 2/4: Подбор инструментов для генерации...
✅ Инструменты подобраны
🏗️ ШАГ 3/4: Создание структуры кода...
✅ Структура создана
💻 ШАГ 4/4: Реализация кода бота...
✅ Код реализован
🔍 ФИНАЛЬНАЯ ПРОВЕРКА: Валидация кода...
✅ Проверка завершена
```

Или напрямую через командную строку:
```bash
python script_bot.py "Бот, который отправляет случайные мемы"
```

### Результат

После выполнения создается файл `generated_bot.py` с готовым кодом Telegram бота.

Для запуска сгенерированного бота:
```bash
# 1. Установите дополнительные зависимости
pip install aiogram

# 2. Добавьте токен бота в .env
echo "BOT_TOKEN=ваш_токен_от_BotFather" >> .env

# 3. Запустите бота
python generated_bot.py
```

### 📊 Логирование

Генератор ботов имеет встроенную систему логирования для отслеживания процесса генерации:

**Уровни логирования:**
- `DEBUG` - Детальная информация для отладки (все шаги, параметры, размеры данных)
- `INFO` - Основная информация о ходе работы (рекомендуется по умолчанию)
- `WARNING` - Предупреждения и потенциальные проблемы
- `ERROR` - Только ошибки

**Настройка:**
```env
# В файле .env
LOG_LEVEL=INFO  # или DEBUG, WARNING, ERROR
```

**Пример вывода логов (INFO):**
```
2026-01-14 15:30:45 INFO __main__ - Запуск script_bot.py
2026-01-14 15:30:45 INFO __main__ - Получено описание: Бот для задач
2026-01-14 15:30:45 INFO __main__ - Запуск генерации Telegram бота
2026-01-14 15:30:46 INFO __main__ - Запуск analysis_chain
2026-01-14 15:30:50 INFO __main__ - Analysis chain завершен: complexity=medium
2026-01-14 15:30:51 INFO __main__ - Запуск tools_selection_chain
2026-01-14 15:30:55 INFO __main__ - Tools selection завершен: db=sqlite, state=FSM
2026-01-14 15:30:56 INFO __main__ - Запуск structure_chain
2026-01-14 15:31:02 INFO __main__ - Structure chain завершен: commands=5, handlers=8
2026-01-14 15:31:03 INFO __main__ - Запуск code_chain
2026-01-14 15:31:15 INFO __main__ - Code chain завершен: 3847 символов, 156 строк
2026-01-14 15:31:16 INFO __main__ - Запуск review_chain
2026-01-14 15:31:20 INFO __main__ - Review chain завершен: is_valid=yes
2026-01-14 15:31:20 INFO __main__ - Бот сохранен в файл: generated_bot.py
2026-01-14 15:31:20 INFO __main__ - Генерация бота успешно завершена
```

**Для режима отладки (DEBUG):**
```bash
# Временно установить уровень через переменную окружения
export LOG_LEVEL=DEBUG  # Linux/macOS
set LOG_LEVEL=DEBUG     # Windows CMD
$env:LOG_LEVEL="DEBUG"  # Windows PowerShell

python script_bot.py "Описание бота"
```

### Примеры запросов для генерации ботов

#### Простой бот
```
Бот, который повторяет все сообщения пользователя
```

#### Бот с командами
```
Бот для получения информации о погоде. 
Команды: /start, /help, /weather город
```

#### Бот с клавиатурой
```
Бот-калькулятор с inline-кнопками для выбора операций: 
сложение, вычитание, умножение, деление
```

#### Бот со сложной логикой
```
Бот для управления задачами:
- /add название - добавить задачу
- /list - показать все задачи
- /done номер - отметить задачу выполненной
- /delete номер - удалить задачу
Задачи хранятся в памяти (словарь)
```

#### Бот с API интеграцией
```
Бот для конвертации валют:
- /convert 100 USD RUB - конвертировать валюту
- Использует API exchangerate-api.com
- Поддерживает USD, EUR, RUB, GBP
```

## 📄 Лицензия

Этот проект создан для личного использования и обучения.

## 📚 Полезные команды

### Проверка установки
```bash
# Проверка версии Python
python --version

# Проверка установленных пакетов
pip list

# Проверка конкретного пакета
pip show openai
```

### Работа с виртуальным окружением
```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/macOS)
source venv/bin/activate

# Деактивация
deactivate
```

### Просмотр файлов
```bash
# Просмотр содержимого .env (Windows)
type .env

# Просмотр содержимого .env (Linux/macOS)
cat .env

# Просмотр сгенерированного бота
type generated_bot.py  # Windows
cat generated_bot.py   # Linux/macOS
```

### Управление зависимостями
```bash
# Установка зависимостей
pip install -r requirements.txt

# Обновление всех пакетов
pip install --upgrade -r requirements.txt

# Установка aiogram для запуска ботов
pip install aiogram
```

## 🎯 Быстрый старт

### Шаг 1: Клонирование и настройка
```bash
cd CASE_PROMPTS
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

### Шаг 2: Настройка .env
```bash
copy .env-example .env  # Windows
# или
cp .env-example .env    # Linux/macOS
```
Отредактируйте `.env` и добавьте свой API ключ.

### Шаг 3: Первый запуск
```bash
python common_prompts.py
```
Выберите промпт 1 (Резюме текста), используйте тестовый вопрос.

### Шаг 4: Генерация первого бота
```bash
python common_prompts.py
# Выберите: 2 → 2
# Введите: "Простой эхо-бот" → END
```

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте правильность настройки `.env`
2. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте логи ошибок в консоли
4. Убедитесь, что виртуальное окружение активировано
5. Проверьте версию Python: должна быть 3.8 или выше

---

**Важные примечания:**
- Файл `.env` уже добавлен в `.gitignore` и не попадет в репозиторий
- Сгенерированные боты сохраняются в `generated_bot.py` (также в `.gitignore`)
- Для работы с ProxyAPI используйте ключ от прокси-сервиса, а не от OpenAI

